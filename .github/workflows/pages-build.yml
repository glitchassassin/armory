name: Pages Build

on:
  push:
    branches: [ "main" ]

jobs:
  kjv:
    uses: ./.github/workflows/build-version.yml
    with:
      domain: "armorer.io"
      version: "eng_kjv"
  bsb:
    uses: ./.github/workflows/build-version.yml
    with:
      domain: "bsb.armorer.io"
      version: "BSB"