---
layout: base
title: "Armory"
---

<main class="container">
    <header>
        <h1>Cambridge KJV</h1>
    </header>
    <article>
        <ul class="toc list-unstyled">
        {% for slug, book in bible.books %}
            <li class="border border-light"><a href="{{slug}}" class="d-block p-2 text-center link-light">{{book.title}}</a></li>
        {% endfor %}
        </ul>
    </article>
</main>