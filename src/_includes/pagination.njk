<nav x-show="paginationTotalPages > 1" aria-label="Search results page">
    <ul class="pagination justify-content-center">
        <li class="page-item">
            <button 
            :class="{'page-link': true, 'disabled': paginationCurrentPage === 0}" 
            @click="prevPage" 
            :disabled="paginationCurrentPage === 0"
            aria-label="Previous"
        >
            <span aria-hidden="true">&laquo;</span>
        </button>
        </li>
        <template x-for="page in paginationPageList">
            <li :class="{'page-item': true, 'active': page === paginationCurrentPage}" >
                <button 
            class="page-link" 
            @click="setPage(page)" 
            x-text="page + 1"></button>
            </li>
        </template>
        <li class="page-item">
            <button 
            :class="{'page-link': true, 'disabled': paginationCurrentPage + 1 === paginationTotalPages}" 
            @click="nextPage" 
            :disabled="paginationCurrentPage === paginationTotalPages"
            aria-label="Next"
        >
            <span aria-hidden="true">&raquo;</span>
        </button>
        </li>
    </ul>
</nav>