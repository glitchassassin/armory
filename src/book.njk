---
layout: base
pagination:
    data: bible.books
    size: 1
    alias: book
    resolve: values
permalink: "{{ book.slug }}"
eleventyComputed:
    title: "Armorer | {{ book.title }}"
    pageHeader: "{{ book.title }}"
---
<main class="container mt-3">
    <header>
        <h1><a href="{{book.slug}}" class="link-light">{{ book.title }}</a></h1>
    </header>
    <article>
        <ul class="toc list-unstyled">
        {% for chapterNumber, chapter in book.chapters %}
            <li class="border border-light"><a href="{{book.slug}}{{chapterNumber}}/" class="d-block p-2 text-center link-light">{{chapterNumber}}</a></li>
        {% endfor %}
        </ul>
    </article>
</main>