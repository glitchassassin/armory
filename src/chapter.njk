---
layout: base
pagination:
    data: bible.chapters
    size: 1
    alias: chapter
permalink: "{{ chapter.slug }}"
eleventyComputed:
    title: "Armory | {{ bible.books[chapter.book].title }} {{ chapter.chapter }}"
    pageHeader: "Chapter {{ chapter.chapter }}"
    prevPage: "{{ chapter.prevChapter }}"
    nextPage: "{{ chapter.nextChapter }}"
---

<div id="content" data-chapter="{{ chapter.slug }}" data-next-chapter="{{ nextPage }}" data-prev-chapter="{{ prevPage }}">
    <main class="container">
        <header class="d-flex sticky-top bg-dark fw-bold p-2">
            <a href="{{bible.books[chapter.book].slug}}" class="link-light">{{ bible.books[chapter.book].title }}</a>
            ⟫
            <a href="{{chapter.slug}}" class="link-light">Chapter {{ chapter.chapter }}</a>
        </header>
        <article class="">
            {{ chapter.content | safe }}
        </article>
    </main>
</div>

{% rollup "src/js/infinite-scroll.js" %}